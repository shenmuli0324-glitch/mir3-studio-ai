[package]
name = "mir3-mcp"
version = "0.1.0"
edition = "2021"

[dependencies]
mir3-domain = { path = "../mir3-domain" }
serde = { version = "1", features = [ "derive" ] }
serde_json = "1"

[dev-dependencies]
easyexcel-xls = "0.1.3"
