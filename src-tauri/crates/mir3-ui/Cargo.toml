[package]
name = "mir3-ui"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = [ "derive" ] }
tree-sitter = "0.25"
tree-sitter-lua = "0.5"

[dev-dependencies]
serde_json = "1"
