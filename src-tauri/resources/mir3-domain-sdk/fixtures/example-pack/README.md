# Runtime-installable SDK example

This declarative `level` pack is accepted by the same Kernel API v1 manifest validator and executable fixture canary used before a candidate pack is activated. It contains no executable module, shell command, React injection, or arbitrary filesystem write.
